spring.application.name=auth
server.port=8082

eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost

cloud.server.port=8080

eureka.client.service-url.defaultZone=http://localhost:${cloud.server.port}/eureka/

jwt.secret=bijenkey

#one hour
jwt.token.validity=3600000
